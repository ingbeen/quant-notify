# quant-notify 프로젝트 가이드라인

> CRITICAL: 특정 폴더의 파일을 작업할 때는 그 폴더의 `CLAUDE.md` 를 먼저 읽습니다.
> 루트 문서는 공통 규칙을, 각 폴더 문서는 그 도메인의 맥락을 담습니다.

## 문서 목적

AI 모델이 이 프로젝트를 정확히 이해하고 일관되게 판단하도록 돕습니다.
사람을 위한 상세 문서가 아니라, **판단 기준과 프로젝트 맥락**을 제공합니다.

---

## 규칙 문서 참고 순서

- **전역 규칙**: `~/.claude/CLAUDE.md` — 사고 절차·수술적 변경·개발 원칙·목표 주도 실행·계획서 선행·검증 지침의 SoT입니다. 이 문서는 quant-notify 고유 맥락만 담습니다
- **설계 근거**: [docs/DESIGN.md](docs/DESIGN.md) — **왜 이렇게 생겼는지의 정본**입니다. 설계에 손대기 전에 읽습니다
- **파이썬 구현**: 전역 `~/.claude/rules/python.md` — 구현 원칙·코딩 표준·로깅 정책. `.py` 파일을 다룰 때 자동 로드됩니다
- **참고 문서**: [reference/README.md](reference/README.md) — 알림이 무엇을 판정해야 하는지의 근거
- **축적 지식**: [docs/MEMORY.md](docs/MEMORY.md) — 작업하며 알아낸 함정·인계사항·환경 노하우. 아래 `@import` 로 **매 세션 자동 로드**됩니다

@docs/MEMORY.md

> **자동 로드는 `Read` 도구로 열었을 때만 걸립니다.** `cat`·`head`·`sed -n` 으로 읽으면
> 그 경로의 규칙이 따라오지 않습니다. **코드 파일은 `Read` 로 엽니다.** 셸은 검색·집계에 씁니다.

---

## 이 프로젝트가 무엇인가 (CRITICAL)

quant 와 verify-lab 의 매매 규칙이 내는 신호를 **텔레그램으로 알리는** 시스템입니다.
**계산은 이 저장소가 직접 하고, 다른 저장소를 import 하지 않습니다.**

### 무상태 — 이 프로젝트의 축

**매일 갱신되는 누적 상태를 만들지 않습니다.** 그것이 이 저장소가 존재하는 이유입니다 —
전신인 quant `src/live/` 가 누적 상태 때문에 거래일 하나를 영구히 잃었습니다
([docs/DESIGN.md](docs/DESIGN.md) §1).

새 저장 요구가 생기면 **두 상태 중 어느 쪽인지 먼저 가릅니다.**

| | 자동 누적 상태 | 사람이 쓰는 파일 |
| --- | --- | --- |
| 갱신 주체 | 매 실행마다 프로그램이 | 사람이, 바뀔 때만 |
| 하루 걸렀을 때 | **영구 손상** | 아무 일도 없다 |
| 판정 | **만들지 않는다** | `state/` 에 둔다 |

**시세를 저장하지 않습니다.** 매 실행마다 필요한 만큼 받습니다. 정본 CSV 를 두는 순간
거래일 공백 검증이 따라오고, 그것이 사고의 구조였습니다.

**알림별로 데이터를 따로 받습니다.** 캐시로 공유하면 알림 사이에 결합이 생기고 신선도
검증이 따라옵니다 — 아끼는 것은 종가 한 개입니다 (§5.4).

### 무엇을 만들지 않는가

아래는 **빠뜨린 것이 아니라 기각한 것**입니다. 되살리기 전에 [docs/DESIGN.md](docs/DESIGN.md) §8 을 읽습니다.

자동 재시도 · 부분 성공 · 값 보간 · 발송 이력 저장 · 공유 DB · `schedule` 백업 트리거 ·
평균단가와 손익 · 역방향 청산 추적 · 옵션 만기일 알림.

---

## 다른 저장소와의 관계

| 저장소 | 관계 |
| --- | --- |
| **verify-lab** | 역방향·원달러 매매 규칙의 **정본**. 스냅샷이 `reference/` 에 있습니다 |
| **quant** | Q-2-2XS 포트폴리오 구성의 정본. **백테스트 전용이며 알림 코드가 없습니다** |

**두 저장소를 import 하지 않습니다.** 판정 규격을 읽고 이 저장소에 새로 작성합니다.
SMA 는 1년치만 받으면 누가 계산해도 같은 값이 나오고, 순위 등락률은 값만 넘겨받으면
신호 가격이 한 줄로 나옵니다 (§3.2).

**`reference/` 는 스냅샷입니다.** 정본이 갱신돼도 조용히 낡습니다. 특히 순위 등락률은
시세를 재수집하면 바뀌므로, 값을 근거로 쓰기 전에 `state/reverse_rank.toml` 의 `data_to` 를 봅니다 —
그 값이 **어느 날까지의 데이터로 매겨졌는지**가 거기 있습니다.

**순위 등락률 값은 이 저장소가 계산하지 않습니다.** 정본은 verify-lab 이고, 사용자가 거기서
재계산해 `state/reverse_rank.toml` 에 직접 커밋합니다. 이 저장소는 **「갱신이 필요하다」까지만**
알립니다 ([docs/DESIGN.md](docs/DESIGN.md) §5.2).

---

## state/ — 사람이 쓰는 파일

`state/` 는 **사람이 편집하고 프로그램은 읽기만** 합니다. git 추적 대상입니다.

- **프로그램이 `state/` 에 쓰지 않습니다.** 쓰려면 워크플로가 커밋을 해야 하고, 그것이
  없애려던 구조입니다
- **파일이 없거나 비어 있어도 알림은 발송합니다.** 해당 블록만 비웁니다
- **스키마 검증은 로딩 시점에 합니다.** 잘못된 값으로 계산에 들어가면 알림이 조용히 틀립니다

---

## 계획서(Plan) 작성이 필요한 경우

원칙: 모든 코드 변경은 계획서를 작성한 후 진행합니다.

예외 (계획서 없이 바로 진행 가능): 오타 수정 · 주석 수정 · 로그 메시지 수정.

계획서 작성 절차와 품질 게이트: **전역 `/impl-plan` 스킬**이 SoT입니다.
여러 저장소가 공유하므로 **이 프로젝트 고유의 값은 아래 절에 있습니다.**

이 규칙은 하네스가 강제합니다 (전역 `~/.claude/hooks/`).

### 계획서 규약 — 이 프로젝트의 설정 (CRITICAL)

#### 1. 검증 명령

| 용도 | 명령 | 실행 시점 |
| --- | --- | --- |
| 품질 검증 | `poetry run python validate_project.py` | **마지막 Phase에서만** |
| 자동 포맷 | `poetry run black .` | 마지막 Phase에서 **적용만** (`--check` 는 쓰지 않습니다) |

#### 2. Scope 에 반드시 적을 것

[docs/COMMANDS.md](docs/COMMANDS.md) 의 업데이트 필요 여부를 명시합니다. 불필요하면 `변경 없음`으로 기록합니다.

**알림 문구를 바꾸면 [docs/DESIGN.md](docs/DESIGN.md) §4 의 예시도 함께 고칩니다.**
문구는 사용자가 매일 읽는 산출물이라, 코드만 바뀌고 문서가 남으면 다음 세션이 옛 형식을 되살립니다.

#### 3. 근거 승격 목적지

계획서를 지우기 전에 남길 가치가 있는 것을 아래로 옮깁니다. 결론뿐 아니라 **근거**를 옮깁니다.

| 옮길 것 | 목적지 |
| --- | --- |
| 설계 결정과 탈락안 | [docs/DESIGN.md](docs/DESIGN.md) |
| 측정·실측 결과와 판정 | `docs/research/` |
| 실행 명령어 | [docs/COMMANDS.md](docs/COMMANDS.md) |

#### 4. 커밋 메시지의 기능명

`기능명 / 내용` 형식을 쓰며, 기능명은 **변경된 파일 경로**로 정합니다.

| 경로 | 기능명 |
| --- | --- |
| `src/notify/data/` | `데이터 / ` |
| `src/notify/alerts/` | `알림 / ` |
| `src/notify/state/` | `상태 / ` |
| `src/notify/notifier/` | `발송 / ` |
| `.github/workflows/` | `워크플로 / ` |
| 문서 | `문서 / ` |
| `.claude/` · 설정 파일 | `하네스 / ` |

---

## 실행 명령어 관리 원칙

> CRITICAL: 모든 실행 명령어는 **[docs/COMMANDS.md](docs/COMMANDS.md) 에서 단일 관리**합니다.
> README.md 와 CLAUDE.md 에는 실행 명령어를 기재하지 않고, 필요 시 그 문서를 참조합니다.

---

## 스크립트 실행 규칙

`scripts/` 의 스크립트와 `validate_project.py` 는 **AI 모델이 직접 실행할 수 있습니다.**

**실제 발송은 사용자가 확인한 뒤 합니다.** 알림은 사용자의 텔레그램으로 나가는 외부 행위입니다.
AI 는 **문구를 표준출력으로 찍는 dry-run 까지만** 하고, 발송 여부는 사용자가 정합니다.

**외부 API 를 부르는 스크립트는 실행 전 목적을 밝힙니다.** yfinance·pykrx·ECOS 는 조회 한도가 있고,
ECOS 는 인증키가 URL 경로에 들어가므로 **로그에 URL 을 그대로 남기지 않습니다.**

---

## 이 프로젝트의 개발 원칙

전역 「개발 원칙」에 더해 둘을 더 봅니다.

- **알림은 읽는 사람이 있다.** 문구·자릿수·강조는 사용자가 읽는 산출물입니다.
  바꾸기 전에 [docs/DESIGN.md](docs/DESIGN.md) **§4.5** 의 예시와 대조합니다.
  **정렬로 뜻을 나르지 않습니다** — 모바일에서 낱말 단위로 접혀 무너집니다 (§4.5)
- **조용한 것과 죽은 것을 구분한다.** 역방향 알림은 신호가 멀면 침묵합니다.
  침묵을 만드는 코드를 건드릴 때는 **점검 줄**(§7.3)이 여전히 그것을 드러내는지 확인합니다
