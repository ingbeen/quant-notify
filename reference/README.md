# reference — 다른 저장소에서 가져온 참고 문서

이 폴더는 **읽기 전용 참고 자료**입니다. 알림이 무엇을 판정해야 하는지의 근거가 여기 있습니다.

## 이 폴더의 규칙

- **여기서 고치지 않습니다.** 정본을 고친 뒤 다시 복사합니다
- **품질 검사(Ruff · PyRight · pytest) 대상이 아닙니다** — `pyproject.toml` 과 `pytest.ini` 에서 제외돼 있습니다
- 문서 안의 **링크와 파일 경로는 정본 저장소 기준이라 열리지 않습니다.** 내용을 읽는 용도입니다
- 알림 구현은 이 문서를 읽고 `src/notify/` 에 새로 작성합니다. 코드를 복사해 오지 않습니다

## 파일

| 파일 | 정본 | 이 저장소가 무엇을 가져오나 |
| --- | --- | --- |
| `역방향_매매_규칙.md` | `verify-lab/docs/strategy/역방향_매매_규칙.md` | §1.1 대상 신호 · **§1.5 시그널 판정 실시간 규격** (순위 등락률, 신호 가격 산식, 여유 1%p, 판정 시각) |
| `원달러_백분위_알림.md` | `verify-lab/docs/strategy/원달러_백분위_알림.md` | §1.2 **평균대비 정의** · §1.3 알림 문구 형식 (네 창, 소수점 자리). **백분위는 쓰지 않습니다** ([../docs/DESIGN.md](../docs/DESIGN.md) §4.5) |

두 파일 모두 **2026-09-05 시점의 스냅샷**이며, 각 파일 최상단에 그 사실이 적혀 있습니다.

## 가져오지 않은 것

| 문서 | 왜 |
| --- | --- |
| `옵션_만기일_매매_규칙.md` | **알림을 만들지 않기로 했습니다.** 규칙 문서 §1.5 의 「알림 없음」 결정을 그대로 따릅니다. 이 저장소가 참조할 일이 없습니다 |

## 스냅샷이 낡는 것을 어떻게 아나

**자동으로는 알 수 없습니다.** 정본이 갱신돼도 이 사본은 조용히 그대로 있습니다.

특히 위험한 것이 **순위 등락률**입니다. 시세를 재수집하면 값이 바뀌는데, `state/reverse_rank.toml` 이
낡은 값을 들고 있으면 알림 기준이 어긋납니다. 다만 **낡으면 안전한 방향으로 틀립니다** —
새 극단이 들어올수록 20위 값은 더 극단적이 되므로, 낡은 값은 임계가 **느슨한** 쪽입니다.
거짓 알림이 늘 뿐 진짜 신호를 놓치지는 않습니다 (역방향 규칙 §1.5 실측).

**정본 저장소에서 시세를 재수집했다면 이 폴더와 `state/reverse_rank.toml` 을 함께 갱신합니다.**

## 언제 지우나

`src/notify/` 의 대응 알림이 완성되고 규격이 코드와 테스트로 고정되면 참고 가치가 줄어듭니다.
다만 **판정 근거를 다시 읽어야 할 일이 계속 생기므로** 서둘러 지우지 않습니다.
